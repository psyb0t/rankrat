{
  "id": "@psyb0t/rankrat",
  "name": "rankrat",
  "version": "0.0.0",
  "description": "Self-hosted, boundary-limited SEO audits, monitoring, internal links, CrUX, ownership, remediation, backlinks, analytics, Cloudflare performance, PageSpeed, and optional local Lighthouse tools. The default server starts the published Rankrat image over MCP stdio.",
  "mcpServers": {
    "rankrat": {
      "transport": "stdio",
      "command": "node",
      "args": ["./bin/cli.js"]
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false
  }
}
